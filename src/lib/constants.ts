/** Employee PINs are always 6 digits (matches backend EMPLOYEE_PIN_LENGTH). */
export const PIN_LENGTH = 6;

/** How long the confirmation screen stays visible before returning home (PRD: 3s, product: 5s). */
export const RESULT_SCREEN_MS = 5000;
